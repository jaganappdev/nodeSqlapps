#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../inspect-brk/bin/inspect-brk" "$@"
  ret=$?
else 
  node  "$basedir/../inspect-brk/bin/inspect-brk" "$@"
  ret=$?
fi
exit $ret
